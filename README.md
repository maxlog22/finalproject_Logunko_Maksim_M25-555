# finalproject_Logunko_Maksim_M25-555

# Проект "Платформа для отслеживания и симуляции торговли валютами"
## Описание проекта:
Проект представляет собой комплексную консольную платформу для управления виртуальными валютными портфелями, позволяющая пользователям работать с фиатными валютами и криптоактивами в единой системе.

### Ключевые возможности платформы:
1. Управление пользователями
- Регистрация и аутентификация с безопасным хранением паролей
- Персонализированные учетные записи с историей операций

2. Виртуальные портфели
- Создание и управление мультивалютными кошельками
- Поддержка фиатных валют (USD, EUR, RUB) и криптовалют (BTC, ETH)
- Реальное отражение балансов в выбранной базовой валюте

3. Торговые операции
- Покупка и продажа активов по текущим рыночным курсам
- Автоматическое создание кошельков для новых валют
- Контроль достаточности средств для операций

4. Актуальная аналитика
- Отслеживание курсов в реальном времени
- Расчет общей стоимости портфеля

### Архитектурные особенности:
Двухсервисная архитектура:
- Core Service — основной сервис с пользовательским интерфейсом (CLI), управлением пользователями, портфелями и транзакциями
- Parser Service — независимый сервис для сбора и обновления курсов валют с публичных API

Безопасность:
- Хеширование паролей с индивидуальной солью для каждого пользователя
- Приватные атрибуты классов с контролируемым доступом
- Валидация всех входных данных

Гибкость и масштабируемость:
- Модульная структура с четким разделением ответственности
- JSON-хранилище данных для простоты разработки
- Возможность расширения списка поддерживаемых валют


## Технологический стек
### Основные инструменты:
- Poetry - управление зависимостями, сборка и публикация пакетов
- Ruff - линтинг и форматирование кода
- Git - контроль версий с применением Gitflow/GitHub Flow
- Makefile - автоматизация рутинных задач

### Системные требования:
- ОС: желательно Linux (Ubuntu, Debian, Kali, Manjaro) или macOS
- Python: версия 3.12 и выше
- Pip: версия 19 и выше
- Poetry: версия 1.2.0 и выше

**Примечание:** Проект был реализован на ОС Windows через Git-Bas MINGW64. Поэтому сценарий с *Makefile не работает* - для работы с poetry команды выполняются напрямую. Также *на Windows отсутствует возможность записывать код через asciinema*, поэтому запись будет осуществленая через обычную запись с экрана компьютера.


## Инструкция по установке poetry и созданию архитектуры проекта

1. Созданим репозиторий на GitHub и клонируем его локально
```bash
$ git clone https://github.com/maxlog22/finalproject_Logunko_Maksim_M25-555.git
'''Cloning into 'finalproject_Logunko_Maksim_M25-555'...
remote: Enumerating objects: 3, done.
remote: Counting objects: 100% (3/3), done.
remote: Total 3 (delta 0), reused 0 (delta 0), pack-r
eused 0 (from 0)
Receiving objects: 100% (3/3), done.'''
```

2. Предварительная проверка наличия установленной свежей версии pip
```bash
$ python -m pip --version
'''pip 25.3 from C:\Users\logun\AppData\Local\Programs\P
ython\Python312\Lib\site-packages\pip (python 3.12)'''
```

3. Установка pipx с помощью pip
```bash
$ pip install pipx
'''Requirement already satisfied: pipx in c:\users\logun\appdata\local\pr
ograms\python\python312\lib\site-packages (1.8.0)
Requirement already satisfied: argcomplete>=1.9.4 in c:\users\logun\ap
pdata\local\programs\python\python312\lib\site-packages (from pipx) (3
.6.3)
Requirement already satisfied: colorama>=0.4.4 in c:\users\logun\appda
ta\local\programs\python\python312\lib\site-packages (from pipx) (0.4.
6)
Requirement already satisfied: packaging>=20 in c:\users\logun\appdata
\local\programs\python\python312\lib\site-packages (from pipx) (25.0)
Requirement already satisfied: platformdirs>=2.1 in c:\users\logun\app
data\local\programs\python\python312\lib\site-packages (from pipx) (4.
5.0)
Requirement already satisfied: userpath!=1.9,>=1.6 in c:\users\logun\a
ppdata\local\programs\python\python312\lib\site-packages (from pipx) (
1.9.2)
Requirement already satisfied: click in c:\users\logun\appdata\local\p
rograms\python\python312\lib\site-packages (from userpath!=1.9,>=1.6->
pipx) (8.3.0)
'''
```

4. Добавление директории pipx в PATH
Эта команда гарантирует, что исполняемые файлы, установленные через pipx, будут доступны из любой точки в командной строке.
```bash
$ pipx ensurepath
'''C:\Users\logun\.local\bin is already in PATH.
⚠️  All pipx binary directories have been appended to PATH. If you
are sure you want to proceed, try again with the '--force' flag.
Otherwise pipx is ready to go! ✨ 🌟 ✨'''
```
5. Для вступления изменений в переменной PATH силу необходимо перезапустить Git Bash.
6. Уставновка Poetry с помощью pipx
```bash
$ pipx install poetry
"""'poetry' already seems to be installed. Not modifying existing
installation in 'C:\Users\logun\pipx\venvs\poetry'. Pass '--force'
to force installation."""
```
6. Проверка завершения установки Poetry
```bash
$ poetry --version
'''Poetry (version 2.2.1)'''
```
7. Внутри корневой директории создадим директорию и файлы .py согласно архитектуре в ТЗ
8. Настройка poetry так, чтобы тот создавал виртуальные окружения в директории проекта 
```bash
$  poetry config virtualenvs.in-project true
```

9. Инициализируем наш python-пакет внутри корневой директории проекта
```bash
$   poetry init 
```

10. Добавим в модуль main.py следующую конструкцию:
def main():
    print("DB project is running!")


if __name__ == "__main__":
    main()

11. Сформируем файл pyproject.toml следующим образом:
[project]
name = "finalproject-logunko-maksim-m25-555"
version = "0.1.0"
description = ""
authors = [
    {name = "Maksim"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
 packages = [
     { include = "valutatrade_hub" },
 ]
 
[tool.poetry.scripts]
valutatrade = "valutatrade_hub.main:main"

12. Запустим модуль main.py:
```bash
$   poetry run python -m valutatrade_hub.main
'''DB project is running!'''
```

13. Выполним установку пакета:
```bash
$   poetry install
'''Installing dependencies from lock file

No dependencies to install or update

Installing the current project: finalproject-logunko-maksim-m25-555 (0.1.0)
'''
```
Poetry создал виртуальное окружение и установил в него пакет.

14. Проверим работает ли скрипт после успешной установки пакета:
```bash
$   poetry run project
'''DB project is running!'''
```

## Итоговая версия иерархии проекта:

```bash
finalproject-logunko-maksim-m25-555/
│  
├── data/
│    ├── users.json          
│    ├── portfolios.json       
│    ├── rates.json               
│    └── exchange_rates.json                  
├── valutatrade_hub/
│    ├── __init__.py
│    ├── main.py
│    ├── logging_config.py         
│    ├── decorators.py            
│    ├── core/
│    │    ├── __init__.py
│    │    ├── currencies.py         
│    │    ├── exceptions.py         
│    │    ├── models.py           
│    │    ├── usecases.py          
│    │    └── utils.py             
│    ├── infra/
│    │    ├─ __init__.py
│    │    ├── settings.py           
│    │    └── database.py          
│    ├── parser_service/
│    │    ├── __init__.py
│    │    ├── config.py             
│    │    ├── api_clients.py        
│    │    ├── updater.py            
│    │    ├── storage.py            
│    │    └── scheduler.py          
│    └── cli/
│         ├─ __init__.py
│         └─ interface.py     
│
├── Makefile
├── poetry.lock
├── pyproject.toml
├── README.md
└── .gitignore
```               

## Файл Makefile
Файл Makefile заполнен, но на Windows не получилось его реализовать, 
поэтому все команды выполнялись через явное их указание
C:\Users\logun\finalproject_Logunko_Maksim_M25-555\Makefile
```bash
install:
    poetry install

project:
    poetry run project

build:
    poetry build

publish:
    poetry publish --dry-run

package-install:
    python3 -m pip install dist/*.whl

lint:
    poetry run ruff check .
```

# Примеры команд из CLI
Сначала необходимо войти в приложение с помощью команды 'valutatrade'

## Аутентификация
### Регистрация нового пользователя
register --username alice --password 1234

### Вход в систему
login --username alice --password 1234

### Выход
logout


## Торговля и портфель
### Показать портфель (в USD по умолчанию)
show-portfolio
show-portfolio --base EUR

### Купить валюту
buy --currency BTC --amount 0.01
buy --currency EUR --amount 100

### Продать валюту
sell --currency BTC --amount 0.005


## Курсы валют:
### Получить курс между двумя валютами
get-rate --from USD --to BTC
get-rate --from EUR --to RUB

### Показать все курсы из кеша
show-rates
show-rates --top 5               # Топ-5 криптовалют
show-rates --currency BTC        # Только Bitcoin
show-rates --base EUR            # Все курсы в EUR
show-rates --json                # Вывод в формате JSON



# Управление Parser Service
### Обновить курсы вручную
update-rates
update-rates --source coingecko     # Только криптовалюты
update-rates --source exchangerate  # Только фиатные валюты

### Показать статистику парсера
parser-stats

### Проверить валидность данных
validate-rates

### Управление автообновлением
scheduler start --interval 60      # Автообновление каждые 60 мин
scheduler status                   # Статус планировщика
scheduler stop                     # Остановить планировщик


# Описание кэша и TTL системы
## Архитектура хранения данных:
data/
├── rates.json              # Текущие курсы (кэш для Core Service)
├── exchange_rates.json     # История всех измерений (Parser Service)
├── users.json              # Данные пользователей
└── portfolios.json         # Портфели пользователей

Как работает TTL:

1. Core Service читает курсы из rates.json
2. Проверяет updated_at каждого курса
3. Если курс старше rates_ttl_seconds → помечается как устаревший
4. Пользователь получает предупреждение "Курс устарел"
5. Рекомендуется выполнить update-rates

## TTL (Time To Live) настройки:
Настройка	            Значение	        Описание
rates_ttl_seconds	    300 (5 мин)	        Максимальный возраст курса в кеше
UPDATE_INTERVAL	        3600 (1 час)	    Интервал автообновления парсером


# Как включить Parser Service и где хранить ключ
1. Установка зависимостей:
```bash
# Установите необходимые пакеты
pip install requests schedule
```
2. Настройка API ключей Через config.json:
```bash
{
  "exchangerate_api_key": "c9e445b3a03e91c0ed51a1c6"
}
```
3. Запуск Parser Service:
### Ручное обновление (разовое):
```bash
# Обновить все курсы
update-rates

# Обновить только криптовалюты
update-rates --source coingecko

# Обновить только фиатные валюты
update-rates --source exchangerate
```

### Автоматическое обновление (планировщик):
```bash
# Запустить планировщик с интервалом 60 минут
scheduler start --interval 60

# Проверить статус
scheduler status

# Остановить
scheduler stop
``` 

### Настройка интервала обновления:
```bash
# Измените значение (в секундах)
python:valutatrade_hub/parser_service/config.py
UPDATE_INTERVAL = 3600  # 1 час
```

4.  Мониторинг работы парсера:
```bash
# Статистика работы
parser-stats

# Проверка качества данных
validate-rates

# Просмотр логов
tail -f logs/actions.log
cat data/parser.log
```

5.  Интеграция с Core Service:
Parser Service автоматически обновляет rates.json, который используется Core Service для:
- Получения курсов (get-rate)
- Расчетов при покупке/продаже (buy/sell)
- Конвертации в портфеле (show-portfolio)

## Демонстрация записи
Т.к. проект был реализован на Windows, то продемонстрировать запись кода через asciinema не получится,
поэтому было принято решение продемонстрировать напрямую через запись экрана. 
Ссылка на запись:https://drive.google.com/file/d/1cMCQUcspmh6X7y4aB_Qi4OOSHxoYzXGT/view?usp=sharing